#include "lists.h"
/**
 * first_to_exct - Function that runs before main.
 */
void first_to_exct(void) __attribute__((constructor));
/**
 * first_to_exct - Function that runs before main.
 */
void first_to_exct(void)
{
printf("You're beat! and yet, you must allow,");
printf("\nI bore myhouse upon my back!\n");
}
